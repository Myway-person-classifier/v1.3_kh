# v1.3 모델 구현 가이드

## 📋 목차
1. [개요](#개요)
2. [살려야 하는 코드 파일](#살려야-하는-코드-파일)
3. [v1.3 재구성 구조](#v1.3-재구성-구조)
4. [구현 단계별 가이드](#구현-단계별-가이드)

---

## 개요

v1.3은 v1.2 모델을 기반으로 **4-Fold 앙상블 기법**을 통해 정확도와 일반화 성능을 향상시키는 구조입니다.

### v1.2 모델 구조 (재사용)
- **SKKUAI 모델 (AvsHModel)**: 문단 단위 계층적 학습
- **AIGT 모델 (InfoNCE Loss)**: Contrastive Learning 지원
- **혼합 전략**: 두 모델의 장점 결합

### v1.3 추가 구조
- **4-Fold Cross-Validation**: 각 Fold별 독립 학습
- **Meta-Learning**: Fold 예측값을 메타 특징으로 활용
- **Meta-Classifier**: MLP/Ridge를 통한 최종 예측

---

## 살려야 하는 코드 파일

### 1. SKKUAI 프로젝트 핵심 파일

#### 📁 모델 관련
- ✅ `2025_SW_Centered_University_Digital_Competition_SKKUAI/models/AvsHModel.py`
  - **용도**: v1.2의 핵심 모델 (문단 계층 구조)
  - **변경 필요**: 없음 (그대로 사용)

- ✅ `2025_SW_Centered_University_Digital_Competition_SKKUAI/models/get_model.py`
  - **용도**: 모델 및 토크나이저 로딩
  - **변경 필요**: AIGT 모델 (Gemma3/Qwen3) 지원 추가

#### 📁 데이터 관련
- ✅ `2025_SW_Centered_University_Digital_Competition_SKKUAI/datasets_ours/text_dataset.py`
  - **용도**: 데이터셋 클래스 (문서 → 문단 분할)
  - **변경 필요**: 없음 (그대로 사용)

- ✅ `2025_SW_Centered_University_Digital_Competition_SKKUAI/datasets_ours/text_collator.py`
  - **용도**: 배치 데이터 전처리 (문단 토크나이징)
  - **변경 필요**: 없음 (그대로 사용)

- ✅ `2025_SW_Centered_University_Digital_Competition_SKKUAI/datasets_ours/get_dataset.py`
  - **용도**: K-Fold 데이터 분할
  - **변경 필요**: 없음 (이미 4-Fold 지원)

#### 📁 학습 관련
- ✅ `2025_SW_Centered_University_Digital_Competition_SKKUAI/train.py`
  - **용도**: 학습 메인 스크립트
  - **변경 필요**: Fold별 학습 및 Logit 저장 로직 추가

- ✅ `2025_SW_Centered_University_Digital_Competition_SKKUAI/text_trainer.py`
  - **용도**: 커스텀 Trainer (BCE + BPR Loss)
  - **변경 필요**: InfoNCE Loss 지원 추가 (AIGT 통합)

- ✅ `2025_SW_Centered_University_Digital_Competition_SKKUAI/utils/compute_metrics.py`
  - **용도**: 평가 메트릭 (ROC-AUC 등)
  - **변경 필요**: Logit 저장 로직 추가

- ✅ `2025_SW_Centered_University_Digital_Competition_SKKUAI/arguments.py`
  - **용도**: 하이퍼파라미터 설정
  - **변경 필요**: AIGT 모델 관련 인자 추가

- ✅ `2025_SW_Centered_University_Digital_Competition_SKKUAI/additional_loss.py`
  - **용도**: BPR Loss 구현
  - **변경 필요**: 없음 (그대로 사용)

### 2. AIGT 프로젝트 핵심 파일

#### 📁 모델 관련
- ✅ `2025-digital-aigt-detection/module/gemma3_seqcls_infonce.py`
  - **용도**: Gemma3 기반 InfoNCE Loss 모델
  - **변경 필요**: SKKUAI Trainer와 통합

- ✅ `2025-digital-aigt-detection/module/qwen3_seqcls_infonce.py`
  - **용도**: Qwen3 기반 InfoNCE Loss 모델
  - **변경 필요**: SKKUAI Trainer와 통합

### 3. 새로 생성해야 하는 파일

#### 📁 v1.3 전용 파일
- 🆕 `v1.3/fold_trainer.py`
  - **용도**: Fold별 학습 및 Logit 생성 관리
  - **기능**: 
    - 4-Fold 순차/병렬 학습
    - OOF Logits (Fold 1) 저장
    - Test Logits (Fold 2,3,4) 저장

- 🆕 `v1.3/meta_dataset.py`
  - **용도**: Meta-Features 데이터셋 생성
  - **기능**:
    - Fold별 Logits 수집
    - `[Num_Samples] x 4` 형태 메타 특징 생성

- 🆕 `v1.3/meta_classifier.py`
  - **용도**: Meta-Classifier (MLP/Ridge)
  - **기능**:
    - 메타 특징 입력 → 최종 예측
    - Cross-Validation 지원

- 🆕 `v1.3/train_fold.py`
  - **용도**: Fold별 학습 스크립트
  - **기능**:
    - 단일 Fold 학습 실행
    - Logit 저장 자동화

- 🆕 `v1.3/inference_fold.py`
  - **용도**: Fold별 추론 스크립트
  - **기능**:
    - 학습된 모델로 Test Logits 생성

- 🆕 `v1.3/meta_train.py`
  - **용도**: Meta-Classifier 학습 스크립트
  - **기능**:
    - 메타 특징 데이터셋 로딩
    - Meta-Classifier 학습 및 평가

- 🆕 `v1.3/meta_inference.py`
  - **용도**: 최종 예측 생성
  - **기능**:
    - Test Logits → Meta-Classifier → 최종 예측

---

## v1.3 재구성 구조

### 📂 디렉토리 구조

```
v1.3/
├── models/                          # 모델 정의
│   ├── __init__.py
│   ├── avsh_model.py                # SKKUAI AvsHModel (재사용)
│   ├── gemma3_infonce.py            # AIGT Gemma3 (재사용)
│   ├── qwen3_infonce.py             # AIGT Qwen3 (재사용)
│   └── hybrid_model.py              # v1.2: AvsH + InfoNCE 통합
│
├── datasets/                        # 데이터 처리
│   ├── __init__.py
│   ├── text_dataset.py              # SKKUAI (재사용)
│   ├── text_collator.py             # SKKUAI (재사용)
│   ├── get_dataset.py               # SKKUAI (재사용, 4-Fold 지원)
│   └── meta_dataset.py              # 🆕 Meta-Features 생성
│
├── trainers/                        # 학습 로직
│   ├── __init__.py
│   ├── hybrid_trainer.py            # v1.2: AvsH + InfoNCE Trainer
│   └── fold_trainer.py              # 🆕 Fold별 학습 관리
│
├── meta/                            # Meta-Learning
│   ├── __init__.py
│   ├── meta_classifier.py           # 🆕 MLP/Ridge Meta-Classifier
│   ├── meta_train.py                # 🆕 Meta-Classifier 학습
│   └── meta_inference.py            # 🆕 최종 예측
│
├── scripts/                         # 실행 스크립트
│   ├── train_fold.sh                # 🆕 Fold별 학습 (4개 Fold 병렬)
│   ├── inference_fold.sh            # 🆕 Fold별 추론
│   ├── meta_train.sh                # 🆕 Meta-Classifier 학습
│   └── meta_inference.sh            # 🆕 최종 예측
│
├── utils/                           # 유틸리티
│   ├── __init__.py
│   ├── compute_metrics.py           # SKKUAI (재사용)
│   ├── arguments.py                 # SKKUAI (수정 필요)
│   ├── losses.py                    # BPR + InfoNCE Loss 통합
│   └── logit_collector.py           # 🆕 Fold Logits 수집
│
├── configs/                         # 설정 파일
│   ├── fold_config.yaml             # 🆕 Fold 학습 설정
│   └── meta_config.yaml             # 🆕 Meta-Classifier 설정
│
└── outputs/                         # 출력 디렉토리
    ├── fold_logits/                 # Fold별 Logits
    │   ├── oof/                     # OOF Logits (Fold 1)
    │   └── test/                    # Test Logits (Fold 2,3,4)
    ├── meta_features/               # Meta-Features Dataset
    │   ├── meta_train.csv
    │   └── meta_test.csv
    └── final_predictions/           # 최종 예측
        └── submission.csv
```

---

## 구현 단계별 가이드

### 🔹 Step 1: v1.2 모델 통합 (Hybrid Model)

**목표**: SKKUAI AvsHModel + AIGT InfoNCE Loss 결합

**작업**:
1. `v1.3/models/hybrid_model.py` 생성
   - AvsHModel 구조 유지
   - Forward에서 InfoNCE Loss 계산 지원
   - Contrastive Learning 옵션 추가

2. `v1.3/trainers/hybrid_trainer.py` 생성
   - TextTrainer 상속
   - BCE Loss + BPR Loss + InfoNCE Loss 통합
   - Loss 가중치 조절 가능

**핵심 코드 스니펫**:
```python
# hybrid_model.py
class HybridAvsHModel(AvsHModel):
    def forward(self, input_ids, attention_mask=None, 
                contrastive_labels=None, lambda_cl=1.0, **kwargs):
        # 기존 AvsHModel forward
        logits, paragraph_logits = super().forward(...)
        
        # InfoNCE Loss 계산 (선택적)
        if contrastive_labels is not None:
            features = self.get_cls_embeddings(...)
            cl_loss = compute_infonce_loss(features, contrastive_labels)
            return logits, paragraph_logits, cl_loss
        
        return logits, paragraph_logits
```

---

### 🔹 Step 2: Fold별 학습 파이프라인

**목표**: 4-Fold로 v1.2 모델 학습 및 Logit 생성

**작업**:
1. `v1.3/trainers/fold_trainer.py` 생성
   - Fold별 학습 루프
   - OOF/Test Logit 저장 자동화

2. `v1.3/scripts/train_fold.sh` 생성
   - 4개 Fold 병렬 학습 (또는 순차)
   - 각 Fold별 독립 실행

3. `v1.3/utils/logit_collector.py` 생성
   - Fold별 Logits 수집
   - 메타 특징 데이터셋 생성

**핵심 로직**:
```python
# fold_trainer.py
class FoldTrainer:
    def train_fold(self, fold_idx, args):
        # 1. 데이터 로드 (K-Fold 분할)
        train_ds, val_ds = get_dataset(args, tokenizer, fold_idx)
        
        # 2. 모델 학습
        trainer = HybridTrainer(...)
        trainer.train()
        
        # 3. OOF Logits 생성 (Fold 1만)
        if fold_idx == 0:
            oof_logits = trainer.predict(val_ds)
            save_oof_logits(oof_logits, fold_idx)
        
        # 4. Test Logits 생성 (Fold 2,3,4)
        if fold_idx > 0:
            test_logits = trainer.predict(test_ds)
            save_test_logits(test_logits, fold_idx)
```

**Fold 실행 스크립트**:
```bash
# train_fold.sh
for FOLD in 0 1 2 3; do
    python train_fold.py \
        --fold_idx ${FOLD} \
        --k_fold 4 \
        --model_type hybrid \
        --output_dir "./outputs/fold_${FOLD}" &
done
wait  # 모든 Fold 완료 대기
```

---

### 🔹 Step 3: Meta-Features 데이터셋 생성

**목표**: Fold별 Logits를 메타 특징으로 변환

**작업**:
1. `v1.3/datasets/meta_dataset.py` 생성
   - Fold별 Logits 로딩
   - `[Num_Samples] x 4` 형태 변환

2. `v1.3/scripts/create_meta_features.sh` 생성
   - Logits 수집 및 CSV 변환

**핵심 로직**:
```python
# meta_dataset.py
class MetaDataset:
    def __init__(self, logit_dir):
        # OOF Logits (Fold 1)
        oof_logits = np.load(f"{logit_dir}/oof/fold0_logits.npy")
        oof_labels = np.load(f"{logit_dir}/oof/fold0_labels.npy")
        
        # Test Logits (Fold 2,3,4)
        test_logits = []
        for fold in [1, 2, 3]:
            logits = np.load(f"{logit_dir}/test/fold{fold}_logits.npy")
            test_logits.append(logits)
        
        # 메타 특징 생성: [Num_Samples] x 4
        self.meta_features = np.stack([oof_logits] + test_logits, axis=1)
        self.labels = oof_labels
```

---

### 🔹 Step 4: Meta-Classifier 구현

**목표**: MLP/Ridge로 최종 예측

**작업**:
1. `v1.3/meta/meta_classifier.py` 생성
   - MLP 옵션
   - Ridge Regression 옵션
   - Cross-Validation 지원

2. `v1.3/meta/meta_train.py` 생성
   - Meta-Classifier 학습
   - Hyperparameter Tuning

**핵심 로직**:
```python
# meta_classifier.py
class MetaClassifier:
    def __init__(self, model_type='mlp'):
        if model_type == 'mlp':
            self.model = MLPClassifier(
                hidden_layers=[64, 32],
                activation='relu',
                dropout=0.2
            )
        elif model_type == 'ridge':
            self.model = RidgeClassifierCV(
                alphas=np.logspace(-4, 2, 25),
                cv=5,
                scoring='roc_auc'
            )
    
    def fit(self, X_meta, y):
        """X_meta: [Num_Samples] x 4"""
        self.model.fit(X_meta, y)
    
    def predict_proba(self, X_meta):
        return self.model.predict_proba(X_meta)
```

---

### 🔹 Step 5: 전체 파이프라인 통합

**목표**: End-to-End 실행 스크립트

**작업**:
1. `v1.3/run_pipeline.py` 생성
   - 전체 프로세스 자동화
   - 단계별 검증

2. `v1.3/scripts/run_all.sh` 생성
   - 모든 단계 순차 실행

**파이프라인**:
```python
# run_pipeline.py
def main():
    # Step 1: Fold별 학습 (5-7시간 × 4)
    print("Step 1: Training 4 Folds...")
    for fold in range(4):
        train_fold(fold_idx=fold)
    
    # Step 2: Meta-Features 생성
    print("Step 2: Creating Meta-Features...")
    create_meta_features()
    
    # Step 3: Meta-Classifier 학습
    print("Step 3: Training Meta-Classifier...")
    train_meta_classifier()
    
    # Step 4: 최종 예측
    print("Step 4: Generating Final Predictions...")
    generate_final_predictions()
    
    print("✅ Pipeline Complete!")
```

---

## 📊 데이터 흐름도

```
[Main Dataset]
      │
      ▼
[Paragraph Decomposition]  (최대 10문단)
      │
      ▼
┌─────────────────────────────────────┐
│  4-Fold Training (v1.2 모델)        │
│  - Fold 0: Train → OOF Logits       │
│  - Fold 1,2,3: Train → Test Logits  │
└─────────────────────────────────────┘
      │
      ▼
[Meta-Features Dataset]
  Shape: [Num_Samples] × 4
  - Column 0: OOF Logits (Fold 0)
  - Column 1-3: Test Logits (Fold 1,2,3)
      │
      ▼
[Meta-Classifier Training]
  - MLP 또는 Ridge
  - Input: [Num_Samples] × 4
  - Output: 최종 예측 (0: Human, 1: AI)
      │
      ▼
[Final Prediction & Submission]
```

---

## 🔧 주요 수정 사항 요약

### 1. Model 통합
- ✅ AvsHModel + InfoNCE Loss 통합
- ✅ Contrastive Learning 옵션 추가

### 2. Trainer 확장
- ✅ HybridTrainer: BCE + BPR + InfoNCE Loss
- ✅ FoldTrainer: Fold별 학습 관리

### 3. 데이터 처리
- ✅ K-Fold 지원 (이미 구현됨)
- ✅ Logit 수집 및 메타 특징 생성

### 4. Meta-Learning
- ✅ Meta-Classifier 구현
- ✅ 최종 예측 파이프라인

---

## 📝 체크리스트

### 필수 파일 재사용
- [x] AvsHModel.py
- [x] text_dataset.py
- [x] text_collator.py
- [x] get_dataset.py (K-Fold 지원)
- [x] gemma3_seqcls_infonce.py
- [x] qwen3_seqcls_infonce.py
- [x] compute_metrics.py

### 새로 생성할 파일
- [ ] hybrid_model.py
- [ ] hybrid_trainer.py
- [ ] fold_trainer.py
- [ ] meta_dataset.py
- [ ] meta_classifier.py
- [ ] meta_train.py
- [ ] meta_inference.py
- [ ] train_fold.py
- [ ] logit_collector.py
- [ ] run_pipeline.py

### 스크립트 파일
- [ ] train_fold.sh
- [ ] inference_fold.sh
- [ ] meta_train.sh
- [ ] meta_inference.sh
- [ ] run_all.sh

---

## ⏱️ 예상 소요 시간

| 단계 | 작업 | 예상 시간 |
|------|------|----------|
| Step 1 | v1.2 모델 통합 | 2-3시간 |
| Step 2 | Fold별 학습 (4개) | 5-7시간 × 4 = 20-28시간 |
| Step 3 | Meta-Features 생성 | 10분 |
| Step 4 | Meta-Classifier 학습 | 30분-1시간 |
| Step 5 | 최종 예측 | 10분 |
| **총계** | | **24-32시간** |

**병렬 처리 시**: Fold별 학습을 4개 GPU에서 병렬 실행 → **8-10시간**

---

## 🎯 최종 목표

1. **정확도 향상**: 4-Fold 앙상블로 일반화 성능 향상
2. **학습 시간 최소화**: SKKUAI + AIGT 효율적 통합
3. **일반화 성능**: Meta-Learning으로 다양한 패턴 학습

---

## 📚 참고사항

- **GPU 리소스**: 4개 Fold 병렬 학습 시 최소 4×A100 필요
- **메모리**: 각 Fold당 약 4-8GB GPU 메모리 필요
- **저장공간**: Logits 저장 시 약 500MB-1GB 필요

